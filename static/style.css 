/* Global styles */
body {
    font-family: 'Comic Sans MS', 'Poppins', sans-serif;
    margin: 0;
    padding: 0;
    background: url('https://i.imgur.com/TY6sLo6.jpg') no-repeat center center fixed; /* Wooden background */
    background-size: cover;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    color: white;
}

/* Container styling */
.container {
    max-width: 90%;
    width: 500px;
    background: linear-gradient(to bottom, #f9f1dc, #f2d3b7);
    border-radius: 20px;
    box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.3);
    padding: 20px;
    margin: 1em auto;
    text-align: center;
}

/* Button styling */
button {
    background: linear-gradient(to bottom, #ffb347, #ffcc33);
    color: white;
    padding: 15px 30px;
    border: none;
    border-radius: 10px;
    font-size: 16px;
    font-weight: bold;
    cursor: pointer;
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.2);
    transition: transform 0.2s, box-shadow 0.2s;
}

button:hover {
    transform: translateY(-3px);
    box-shadow: 0px 6px 10px rgba(0, 0, 0, 0.3);
}

button:active {
    transform: translateY(2px);
    box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.2);
}

/* Timer styling */
.timer {
    font-size: 2rem;
    font-weight: bold;
    color: #ff5722;
    margin: 20px 0;
}

/* Labels and inputs */
label {
    display: block;
    font-size: 1.2em;
    margin-bottom: 10px;
    color: #5a3d2b;
}

input {
    padding: 10px;
    border-radius: 10px;
    border: 2px solid #ffb347;
    font-size: 1em;
    margin-bottom: 20px;
    width: 80%;
}

/* Restart Button */
.restart {
    background: linear-gradient(to bottom, #f24c4c, #e03e3e);
    color: white;
}

.restart:hover {
    background: linear-gradient(to bottom, #e03e3e, #c13232);
}

/* Mobile responsiveness */
@media (max-width: 600px) {
    button {
        padding: 10px 20px;
        font-size: 0.9em;
    }

    .container {
        width: 95%;
    }

    .timer {
        font-size: 1.5rem;
    }
}